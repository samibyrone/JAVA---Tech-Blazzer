import java.util.Scanner;

public class TableOfSquareCubes {

	public static void main (String[] args) {
		
		Scanner input = new Scanner(System.in);

		int number;
		int square;
		int cubes;
		
		System.out.println("\n number\t Square\t Cubes\n");

		number = 0;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));
		
		number = 1;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));
		
		number = 2;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 3;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 4;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 5;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 6;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 7;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 8;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 9;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

		number = 10;
		System.out.printf("%d \t %d \t %d\n", (number), (number * number), (number * number * number));

	}
}


// Create a scanner
// Create a variable
// Number for square and cubes calculation
// Display output result